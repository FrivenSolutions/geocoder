node_modules/
.tmp/
*.zip

# The gazetteer. 244 MB of generated data, published as a release asset rather than
# committed: git keeps every version forever, so each rebuild would add another 244 MB to
# history permanently. See the README for where to get it.
dist/data/
